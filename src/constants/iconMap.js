import { Monitor, Share, Code, Users, Video, Clipboard, CheckSquare, FileText, CameraIcon } from 'lucide-react';

export const iconMap = {
    Monitor,
    Share,
    Code,
    Users,
    Video,
    Clipboard,
    CheckSquare,
    FileText,
    CameraIcon
};